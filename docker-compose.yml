version: "3.6"
services:
  harbor:
    build: .
    command: "mix phx.server"
    ports: 
      - 4000:4000
    networks:
      - db
      - harbor
    depends_on:
      - db
    volumes:
      - type: bind
        source: ./
        target: /usr/harbor
  
  db:
    image: postgres
    restart: always
    networks:
      - db
    ports:
      - 5432
    environment:
      POSTGRES_PASSWORD: harbor #TODO

networks:
  harbor:
  db: